task build(type: GradleBuild) {
    group 'application'
    setTasks(
            [
                    ':barcode-app-client:buildClient',
                    ':barcode-app-server:jar'
            ])
}