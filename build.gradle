task build(type: GradleBuild) {
    group 'application'
    setTasks(
            [
                    ':barcode-app-client:cleanBuildAndDeploy',
                    ':barcode-app-server:jar'
            ])
}